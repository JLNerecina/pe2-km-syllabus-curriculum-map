# Prompt Log – Faustino, Felix Luis A.
**Project:** Syllabus & Curriculum Map (Knowledge Mapping)  
**Branch:** dev, feature/*, fix/\*
**Role:** Fullstack Developer

## Entry 1
**Date:** April 1, 2026
**Task:** feature/scaffold

**Prompt given to AI:**
"Let's plan for the scaffold from the tech stack of the site's main functionality:

A Knowledge Mapping web application that visualizes how courses connect — showing prerequisites, skills acquired, and knowledge built across an entire academic program.

This website will be used by students, teachers & faculty alike, and managed by admins. It will be similar to a framework like moodle, though this site will be used for a more specific knowledge distribution method."

**What the AI produced:**  
1. Drafts for the technical implementation of the scaffolding for the Knowledge Mapping application. Tech stacks were considered and decided as follows: React for frontend, Vite for building, Tailwind v4 for styling, Supabase for the authentication and database backend.
2. Built the project from the ground-up given the tech-stack considerations.

**What I changed / improved:** 
1. Used foundational React instead of Next.js as the scope of the current project does not demand SEO optimization and the performance of a production/business-ready app catered for a wide scale of users.
2. Optimized the folder structure by creating directories within /src, effectively improving this build's purpose as a scaffolding by making it easier for other developers to build upon.

**What I learned / decided:**  
The current iteration of the scaffolding is enough for other developers to build over, which will most likely be the UI developer. 


## Entry 2

**Date:** April 4, 2026
**Task:** feature/scaffold -> dev

**Prompt given to AI:**  
"Attached is the UI template provided by the UI/UX developer. Please improve the scaffolding by integrating these UI elements as placeholders - do not make anything functional just yet."

**What the AI produced:**  
A faithful recreation of the provided UI template, complete with sidebar, navbar, and a main page. Multiple buttons, widgets, and gizmos are found in the sidebar, navbar, and the main page.

**What I changed / improved:**  
Some buttons routed to different pages. These pages are not included in the sprint scope just yet, so additional prompts were needed to further clean the slate. Buttons were ensured to be non-functional and widgets were verified to be placeholders. The main page is wiped completely, as the content itself is also not a part of the current sprint.

**What I learned / decided:**  
The scaffolding itself is now completed. The addition of the UI elements from the work of the UI developer will ensure that everyone working over the scaffolding will be on the same page, and to ensure that functionalities built by several developers will not conflict with one another.

## Entry 3

**Date:** April 19, 2026
**Task:** Building Login Page

**Prompt given to AI:** 

Please create a login page:

NOTE: I have not set up authentication on supabase's side yet. Walk me through what I need to set up.

- The login page will be minimal, ONLY allowing google authentication as the login method. This means there will be no email/username & password fields, only logging in through a google account.
- Successful logins will redirect to the main page.

**What the AI produced:**  
The AI produced a premium, dark-themed login interface featuring glassmorphic components and ambient background gradients. It established the underlying authentication architecture by creating a ProtectedRoute component, updating the app's routing, and providing a step-by-step artifact guide for configuring Google OAuth in the Supabase dashboard.

**What I changed / improved:** 
The initial design was simplified to remove the "purply" ambient effects in favor of a clean, white aesthetic. I also improved the user experience by requesting that the authentication process occur in a separate popup window rather than redirecting the main application tab, and I had the AI implement a functional logout mechanism for the main dashboard.

**What I learned / decided:**  
When creating an organization-based app that already has an established email domain, it is best to simplify the app and just use Google OAuth for authentication configuring the app to reject external accounts. Furthermore, web apps, without explicit UI instructions, will almost always default to a purple styling alongside a glassmorphism effect, without fail.

## Entry 4

**Date:** April 19, 2026
**Task:** Overhauling UI and Tweaking Login Page

**Prompt given to AI:** 
"Simplify the login page:

Remove the purply background. Keep it white, clean and simple.
When logging in, have the auth window pop up as a seperate window instead of taking up the same tab as the app.
Make the logout button on the main page functional."

**What the AI produced:**  
The AI refactored the login page into a minimal, high-contrast white UI. It implemented skipBrowserRedirect logic to launch a centered popup for Google Auth and added a listener to the AuthContext to automatically close the popup once a session is established. Finally, it converted the sidebar's static logout link into a functional button using the useAuth hook.

**What I changed / improved:** 
No further revisions have been requested for this specific implementation.

**What I learned / decided:**  
Nothing remarkable

## Entry 5

**Date:** April 23, 2026
**Task:** 2nd Overhauling UI of Login Page, With Guidance From UI/UX Dev

**Prompt given to AI:** 
"Read the directory Stitch/cics_login_definitive

I would like you to overhaul the UI of the login page using the html and the attached image as the reference. Remove the Support and Policies button, and the OR divider between those buttons and the sign in with google button, however."

**What the AI produced:**  
A complete visual overhaul of the Login.tsx page. The AI migrated a sophisticated "cosmic" dark-mode design from a reference HTML file into the React environment. This included setting up Tailwind v4 theme tokens (colors, spacing, typography) in src/index.css, importing external Google Fonts and Material Symbols in index.html, and rebuilding the login component with glassmorphism effects while explicitly omitting the Support/Policies buttons and the "OR" divider.

**What I changed / improved:** 
No immediate revisions were requested for this implementation; the overhaul was completed in a single pass based on the initial requirements.

**What I learned / decided:**  
LLMs are decent at following instructions based on using prefabricated structures as building reference.

## Entry 6

**Date:** April 23, 2026
**Task:** Overhauling UI and Infrastructure of Student Dashboard, Mainly /tracker and /map pages

**Prompt given to AI:** "Read the directory Stitch/student_dashbard_definitive. I would like you to overhaul the UI of the dashboard page using the html and the attached image as the reference. Remove the top card that says "My Academic Journey" and prompts to save and download the map.

This will be the design implementation of each component in the references:

#Sidebar
- There will be two elements here, namely:

##Tracker
- This will be the main page. This page will contain what the student has taken and is currently taking.
- WIthin vertical tabs are various courses sorted by two semesters within a year. A column represents a year, and inside that year is the 1st and 2nd semester, and within those semesters are courses/subjects (use the term courses) with corresponding units. Each semester, there is a limit on the units that can be taken.
- There will be a set of buttons above these tabs that will represent each year, from 1st to 4th.
- These buttons can only be clicked in order, going from 1st to 4th. You cannot click 4th when 3rd is not selected.
- Below these set of buttons are two buttons representing semester. The same order rule applies as above.
- Reserve space below these buttons for editing and saving changes.
- These buttons are how a student configures the courses they have taken in their own tracker in order. The workflow is as follows:

###Tracker - Workflow
- A new user/student has the 1st year selected by default. If they are 1st year, they are able to select courses that they are taking.
- They cannot select too many courses based on the units against the unit limit per semester. However, As the warning appears, so is the option to overload, which provides the user with more units. The new limit, however, cannot be overriden anymore.
- After selecting courses, the user can then save. The selected courses will not be editable anymore, unless the edit button was clicked.
- Say, a third year user comes along. The only difference in the workflow is that they will have to go through 1st year 1st sem, save, 1nd year 2nd sem, save, so on and so forth, opting to overload if they have in the past, until they have caught up in their current curriculum. 
- If they decide to backtrack years or sem, however, undo all the changes they have made, but not without a warning first telling them that any changes past that selection will be undone.

##Tracker
- However, some courses are locked out through prerequisites. A student must undertake certain courses before taking a certain course.
- Each selectable course must have a button inside them that will pull up a sidebar to the right. Include the details of the course. Refer to the stitch documentation for the design reference, but take note of the prerequisites and the courses with this course as the prerequisite.

##Map
- This will be a more compact and comprehensive view of the user's progress as defined in the tracker section. This will contain the same structure as the tracker page, but the styling is compact enough to fit a single page without scrolling. Courses not yet attained will not appear here.
- This is updated live as changes in the dashboard are saved.
- There will be an option to save the map as a PDF file.

#Database
- With the provided context above, formulate a robust database that caters to the needs of this application.

##Users
- There are three authorization levels: student, faculty, admin, superadmin

- A user has these properties: ID number (XX[two lastdigits of a year]-XXXXX-XXX), name, their insitutional email (@neu.edu.ph), their authorization level.

##College
- The scope of this project should encompass various departments, their programs and the corresponding courses under them.
- The hierarchy goes as follows:
- - Department: e.g. College of Informatics and Computing Studies (CICS)
- - Program: (under CICS) BS Computer Science (BSCS), BS Entertainment and Multimedia Computing (BSEMC), BS Infomation Technology (BS IT), BS Information Systems (BS IS)
- - Courses: (under BSCS) Introduction to Computing (CCC111-18, 3 units), etc.
- A comprehensive list of courses under the BSCS program will be provided. For now, seed the database as needed.

Discard the root page of the app. instead, anyone accessing the website without a directory would be redirected to /tracker."

**What the AI produced:** 
The AI performed a complete architectural overhaul. It moved navigation to a modernized top bar, implemented a universal scrollable curriculum view with strict sequential validation, and built a destructive "Edit Term" system that handles cascading progress deletion with a confirmation modal.

**What I changed / improved:** 
- Columnar Layout: Divided the universal course list into a two-column grid (Semester 1 on left, Semester 2 on right) per year to maximize screen real estate.
- Container Scaling: Removed max-width constraints on the list to allow the curriculum cards to span the full width of the dashboard.
- Backlog Accessibility: Modified the selection logic to allow users to select "missed" courses from previous years/semesters as backlogs for their current term.

**What I learned / decided:**  
- It is necessary to provide the real-world application of the software as context.
- Fixed sidebars for small-scale apps like this are, more often than not, just a waste of valuable screen space that could be allocated to the main content of the web page.

## Entry 7

**Date:** April 23, 2026
**Task:** Improving Student Dashboard System

**Prompt given to AI:** 
"Some additional features to be implemented and bugs to be dealt with:

#Finishing the Curriculum
- If a user is in 1st year 1st sem and selects two courses, it should reflect as "Taking" in the map instead of already passed. It will only turn into "Passed" if it is not retaken in the next semester.
- To ensure that 4th year 2nd sem courses are eventually marked as passed, have a section appear below the year/semester selection and term summary that prompts the user if they have finished the curriculum to finalize and mark the 4th year 2nd sem courses as passed.
- Ensure the database reflects these changes.

#Retaking a course multiple times
- A user can fail and retake a course multiple times. When retaking a course meant for 1st semester in the 2nd, the 1st sem course represented in the map is rightfully marked as failed. However, subsequent failures are not represented at all. On the next year, if that same course is taken again, the 2nd sem course represented in the map still remains as passed.
- Ensure the database reflects these changes, if applicable."

**What the AI produced:**  
The AI implemented a complex status lifecycle in the database. Courses now debut as enrolled, automatically upgrade to passed in subsequent terms if not retaken, and precisely track historical failures by identifying the most recent past instance during retake attempts. A "Finalize Curriculum" utility was also added for graduating students.

**What I changed / improved:** 
This major logic implementation was accepted without granular UI revisions in the subsequent prompt.

**What I learned / decided:**  
Nothing remarkable.

## Entry 8

**Date:** April 23, 2026
**Task:** Implementing Prerequisite Logic on Dummy Courses

**Prompt given to AI:** 
"Please fully implement the prerequisite functionality. 

- Currently, a user is able to take calculus II without having taken calculus I. There should be a warning popup that describes the error.
- Additionally, when selecting a specific course, the sidebar must reflect the prerequisites of this course and the courses that has this chosen course as the prerequisite."

**What the AI produced:**  
The AI implemented the core backend logic for prerequisite verification, state management for course_prerequisites fetched from Supabase, and a basic information sidebar that listed prerequisites and dependent courses.

**What I changed / improved:** 

- Error Visibility: Modified standard alerts to a premium floating/fixed notification section that follows the screen during scrolling.
- Academic Planning: Enabled the selection of future-semester courses while maintaining strict prerequisite enforcement.
- Visual Feedback: Implemented a "Locked" state for courses with unfulfilled prerequisites, featuring dimmed cards, lock icons, and high-visibility "Prerequisites not done" badges with pulsing indicators.
- Sidebar Enhancements: Added "Passed" and "Taking" status badges to prerequisite lists and standardized the "Required For" section to use a consistent card-based layout.
- Contextual Warnings: Added a yellow outline warning that appears below the semester selector when viewing "Closed" (completed) terms.

**What I learned / decided:**  
It may be useful to prototype the application first using mock data or a small subset of the real-world data your application is expected to use.

## Entry 9

**Date:** April 25, 2026
**Task:** Inclusion of All CICS Courses

**Prompt given to AI:** 
"Below is the complete list of the courses within the CS program. Please configure the database accordingly, replacing the placeholder courses. You are the one to define the which course if the prerequisite of which. Let me know if there are followup operations or questions that you'd like me to verify. [Full BSCS Course List Provided]"

**What the AI produced:**  
The AI performed a total cleanup of the existing database placeholders and generated a comprehensive SQL script to inject 78 unique courses for the BS Computer Science program, including 53 logically mapped prerequisite relationships across all academic years.

**What I changed / improved:** 
No immediate revisions were made to the data structure at this stage, as the task was focused on the initial baseline data entry.

**What I learned / decided:**  
Nothing remarkable.

## Entry 10

**Date:** April 25, 2026
**Task:** Integration of Summer Semesters

**Prompt given to AI:** 
"To properly integrate the summer section into the UI, here are the design parameters:

A summer semester would still be a part of a year (in this case, 3rd year). To ensure uniformity, all years will have a 3rd semester dedicated for summer.
- By default, a student enrolling into a summer class is entirely optional UNLESS the corriculum itself contains a dedicated summer semester, which it does for our CS example.
- To further define the summer enrolment option:
- - On the tracker page, right next to the buttons Semester: 1st Semester, 2nd Semester, there must be another button called Summer
- - Like other sequential buttons, this cannot be clicked if a user has not done 2nd semester yet.
- - When this button is selected, an alert will pop up (Use the same one that alerts people if the semester they have selected is already locked) letting them know that if the student does not wish to enroll into a summer semester, they can ignore it and click Save Progress to move on.

 However, how the app handles summer semesters visually should be different:
- Under a year, sem 1 and 2 are side by side under the Year header.
- For summer, it must exist  as a section BELOW BOTH semester sections, taking up both columns."

**What the AI produced:**  
The AI updated the application logic to support a 3rd "Summer" semester. This included adding a UI selector, implementing an informative alert regarding the optional nature of summer enrollment, and refactoring the curriculum list to render summer courses as a full-width section below the standard semester columns.

**What I changed / improved:** 
Specific refinements were made to the Map view to ensure that empty or "skipped" summer terms are hidden entirely from the PDF export to maintain a compact document layout.

**What I learned / decided:**  
A drastic change such as this (inclusion of a 3rd semester instead of the typical 2) this late into the project cycle is not ideal. Thankfully, the app has not grown substantially since then and the entire application still remains within Gemini's context window, but this technical debt that exists since the application of fundamental structures and design must never be allowed to live for this long.

## Entry 11

**Date:** April 25, 2026
**Task:** Tweaking Prerequisite Logic

**Prompt given to AI:** 
"On another matter, please fix the prerequisite logic:

- A course(subject) cannot be a prerequisite of another coures within the same semester. ensure that ALL courses in a semester, assuming all courses on previous semesters were all completed, can all be taken at the same time.
- Remove prerequisites of all minor subjects. This applies to all courses not starting with C (e.g. KONFIL-18)"

**What the AI produced:**  
The AI executed a targeted SQL cleanup that removed any prerequisite dependencies where the target and prerequisite courses shared the same year and semester. It also stripped all prerequisite requirements from minor subjects (General Education and PE courses).

**What I changed / improved:** 
The AI verified the database state to ensure cross-semester dependencies for major subjects remained intact while the new "simultaneous enrollment" rule was applied.

**What I learned / decided:**
Nothing remarkable.  

## Entry 12

**Date:** April 27, 2026
**Task:** Optimizing Curriculum List Navigation

**Prompt given to AI:** 
"There is a fatigue issue where a student must scroll an exhaustingly long way until reaching the course list of higher years such as 3rd or 4th. To ensure this is mitigated:

- Check which year is selected.
- Whichever year is selected, its corresponding section on the Universal Course List (Change this into just "Course List" as well, by the way) would shift into the top of the list. How the course list would be redrawn is as follows:
  - A user selects 2nd year after being done with all of 1st year.
  - The 1st year section, finished already, is dropped all the way at the bottom of the list. That would mean, when scrolling down, the user will see 2nd, 3rd, 4th year, then the 1st year."

**What the AI produced:**  
The AI implemented a dynamic year-reordering mechanism in Tracker.tsx. It ensures that when a year is selected, that year's courses appear at the top of the list, followed by subsequent years, with previously completed years moved to the bottom. The "Universal Course List" was also renamed to "Course List".

**What I changed / improved:** 
The AI's initial sorting logic was over-engineered using a complex map and sort approach. This was revised and simplified into a straightforward array construction logic for better code maintainability.

**What I learned / decided:**  
To ensure accessibility of an application, always emulate the average user as a non-technical person that does not know how to navigate as well as developers. This will increase awareness for building intuitive applications that serve the user instead of being wrangled by them just to work.

## Entry 13

**Date:** April 27, 2026
**Task:** Optimizing Curriculum List Navigation

**Prompt given to AI:** 
"The app needs to track which courses are in the "taking" status within the tracker page, not just the map page. To implement this: Instead of just greying out courses that have been selected and saved in a semester, outline them with a bright blue glow and place a tag in them (the same tag that is used for marking locked courses due to prerequisites) that says currently taking."

**What the AI produced:**  
The AI added logic to identify courses with an 'enrolled' status (saved courses) and styled them with a blue glow effect and a "Currently Taking" badge, using the design language of the prerequisite warning tags.

**What I changed / improved:** 
The visual "glow" effect was removed to keep the interface clean, while keeping the "Currently Taking" badge for functional clarity.

**What I learned / decided:**  
Visual identification is key to controlling the user flow, attracting attention to elements that actually matter to the user at the current moment.

## Entry 14

**Date:** April 27, 2026
**Task:** Improving Monitoring Page

**Prompt given to AI:** 
"On the monitor page, I'd like the following changes:

#Selecting Categories
- Academic Programs must be a drop down
- Year Levels selection buttons must be within a single row
- Students section can only expand until the bottom of the viewport until scrolling is enabled. Place a searchbar right on top.

#User Profile Section
- Remove Gen. weighted average card
- remove status: cleared card
- ensure units completed card and Degree Completion baraccurately reflects what is on the database
- View Curriculum Grid should instead say Curriculum Map, and it should pull up a separate tab that is similar to /map, but for the selected student instead. This map, like /map, can be downloaded and saved.
 - Faculty who has the ability to edit curriculums trackers of students, or any admins, must be able to see an edit button in this student map view, which will redirect that new tab to a page like /tracker where admins and faculty can manually edit what they are taking."

**What the AI produced:**  
The AI refactored the Monitor page to include a premium dropdown for programs, a single-row layout for year levels, and a flexible, searchable student list. It also implemented the logic to fetch accurate unit statistics for selected students and established parameterized routing for administrative viewing of specific curriculum maps and trackers.

**What I changed / improved:** 
I specified that total units should include failed course retakes and that all styles must be congruent. I requested the removal of the "Curriculum Compliance Matrix" placeholder and the Year Level filter. Finally, I corrected a tab-closure bug in the Auth context and redirected the "Edit Tracker" flow to stay within the same tab while appending the student's name to the header for clarity.

**What I learned / decided:**  
Nothing remarkable.

## Entry 15

**Date:** April 27, 2026
**Task:** Improving Monitoring Page

**Prompt given to AI:** 
"On the user section back on /monitor,

Remove the completion section on the Units Completed tab. Additionally, create another card right beside it that specifically denotes the units they are taking. the existing units completed card should only reflect units that have actually been passed."

**What the AI produced:**  
The AI updated the statistics calculation to separate "Units Passed" from "Units Taking." It replaced the single completion card with two distinct, side-by-side metric cards (green for passed, cyan for taking) and updated the degree completion progress bar to focus solely on passed units.

**What I changed / improved:** 
I requested the removal of the "Download Record" button from the student profile view as it was a non-functional placeholder.

**What I learned / decided:**  
Nothing remarkable.


## Entry 16

**Date:** April 30, 2026
**Task:** Improving Manage Users Page

**Prompt given to AI:** 
"Managing users would also mean editing properties of users as defined in their corresponding profiles table in the database.

Under User List, 
- add another column "Edit Curriculum" that is a switch, toggling a faculty member's ability to edit curriculums, not just view them. Ensure that this is reflected on the Edit Curriculum button when viewing a student's map.

- add another action button "Edit" which will turn the entries in the row into editable fields. Strings, such as names, can be typed on, while roles will be a dropdown. This will have all editable fields of a user, pre-filled by existing data. While editing, this button will turn into a green "SAVE" and another gray button called "DISCARD".

#Role
- Proper Authorization levels still apply here, such as admins not being able to elevate anyone to admin, and superadmins not being able to elevate anyone to superadmins.

#Search
- Add a search bar on top of the User List that searches through all fields.

#Show only ___
- On the User List table, on the topright corner of the header on the same row as Filter and Sort buttons, add a cycling button:
- "All Users", "Active Only", "Blocked Only""

**What the AI produced:**  
The AI performed a comprehensive overhaul of the UserListTab, implementing a multi-field search bar, a tri-state status cycle button, and an "Edit Curriculum" permission toggle. It built a robust inline editing system for user rows with role-restricted dropdowns that honor the institutional hierarchy (e.g., admins cannot create other admins).

**What I changed / improved:** 
I revised the canEditUser logic to allow users to edit their own profile information (Name, ID number, and Program), while ensuring the "Role" field remains disabled during self-editing to prevent accidental demotion or security bypasses.

**What I learned / decided:**  
There generally are rule of thumbs that a developer must follow when it comes to the management of authorization through different authorization levels.
- Do not let a user demote/elevate their own role/deactivate themselves
- Do not let a user elevate another user to the same role as them


## Entry 17

**Date:** April 30, 2026
**Task:** Initializing Manage Users page

**Prompt given to AI:** 
"Implement the manage users page using the reference to Stitch/admin_management_definitive. While you take in the structure and idea, there are some changes:

Reminder: all dynamic elements presented on the stitch folder must be functional

#User List

- Everything as is, ensure filter and sort are functional, and ensure the page allows 10 entries at a time, with chevrons controlling the page flow.

#Bulk Enrollment
- Remove Bulk Enrollment Protocol from the main page.
- Add Pre-Authorize Button (and remove "Add Single User" button) into the tab list on the top right, right after Bulk Enrollment.

#Pre-Authorize
- A form will prompt an admin into adding a faculty member, complete with all the information the system needs. Refer to the database, profiles table, using MCP, but the important part is taking in their email to allow for an authorized login.

#Bulk Enrollment
- This is where the Bulk Enrollment Protocol section, that was removed from the main page, should be. It allows admins to arrange a list of students, their student numbers and name, and especially email, alongside a configuration of where they will be enrolled. refer to the database for what these configurations might be.
- This list will be expandable, as defined in the stitch documentation."

**What the AI produced:**  
The AI produced a comprehensive three-tabbed "Manage Users" interface. It included a functional data table for user management, a pre-authorization form for manual faculty entry, and a bulk enrollment system for batch processing student accounts with audit log instrumentation.

**What I changed / improved:** 
The system was improved to handle duplicate email prevention during bulk enrollment. Manual audit logging was added to ensure every administrative action was tracked in the database, and the audit log schema was refined to store detailed metadata about course and status changes.

**What I learned / decided:**  
Nothing remarkable.

## Entry 18

**Date:** April 30, 2026
**Task:** Improving the Intuitiveness of the Manage Users Page

**Prompt given to AI:** 
"On the User List table under Manager users:

The current edit functionality is severely restricting the table by having columns not all users benefit from (faculty vs everyone else, for example).

#Changes:

- Remove Program, Edit Curriculum from columns.
- Remove Block button from actions.
- Remove the inline edit functionality when clicking edit. instead:

##Editing a user

- Clicking edit will popup a modal in front of the screen, outlining the profile and its fields that one can edit (prefilled by current data). Like inline editing, dropdowns for roles and string edits for names or ID.
- Faculty: 
  - editing a faculty will provide an option to provide them perms to edit curriculum.
  - Refer to the database, specifically the faculty overseen programs table. You are to allow a faculty member to have multiple progams to oversee and monitor."

**What the AI produced:**  
The AI refactored the User List into a cleaner, simplified table and implemented a sophisticated editing modal. This modal introduced a multi-select system for Faculty overseen programs, complete with a secondary Department filter to manage large program lists efficiently.

**What I changed / improved:** 
The "Block" functionality was re-integrated as an account status card within the edit modal rather than a separate table action. Additionally, the UI hierarchy was revised by moving this status card to the bottom of the modal content area to ensure it didn't disrupt the primary data entry flow. The list length was also adjusted from 10 to 8 users per page for better vertical fit.

**What I learned / decided:** 
Again, it is important to truncate the UI with featureless elements. 

## Entry 19

**Date:** April 30, 2026
**Task:** Implementing Audit Trail Tab Under Manage Users Page

**Prompt given to AI:** 
"On the 3 tabs within Manage Users page, create another tab called "Audit Trail".

#Audit Trail
- This page will keep track of ALL types of changes made by authorized users, including:

##Faculty
- Certain authorized faculty members making edits to student's curriculum

##Admin
- As defined above
- Managing users, including editing attributes and changing roles, etc
- Bulk-enrollment
- Pre-authorization

##Superadmin
- As defined above

Note that, with any changes defined above, they must be logged in DETAIL, noting which is changed, and what are the original and modified values of that change. This will also be implemented into the database."

**What the AI produced:**  
The AI implemented the foundational audit_logs database table and a series of Postgres triggers to automatically capture INSERT, UPDATE, and DELETE events on key tables (profiles, student_courses, etc.). It also produced the initial AuditTrailTab.tsx with basic filtering, search, and CSV/JSON export functionality.

**What I changed / improved:** 
Initially, the AI implemented per-row database triggers which caused "log flooding" (e.g., one bulk enrollment of 10 students created 11 separate log entries). I directed a major architectural shift to remove the noisy triggers and move to consolidated client-side logging, where one logical action creates exactly one rich log entry.

**What I learned / decided:**  
Nothing remarkable.

## Entry 20

**Date:** April 30, 2026
**Task:** Audit Trail Tweaks

**Prompt given to AI:** 
\[Attached images contain the Audit Trail logs having multiple entries for one batch-processing, auditing them as individual jobs.\]
"Read the images. It appears bulk enrollment does not have details and seemingly spawns in multiple pre-authorization logs. This is bad for visibility as bulk-enrollments will flood the logs. Adding courses also appears to do it multiple times even if I saved progress for less than 3 times.

(The agent that worked on these was terminated, so please verify if everything is complete.)

Instead, since we are going for a drop-down approach, it would be better if we combined everything that came from a single change into that dropdown.

#Bulk Enrollment
- It appears that bulk enrollments are just pre-authorizations in burst. With that in mind, perhaps you are able to create a table instead within that drop-down that has id number, name, email, and in the header would indicate to which program they were enrolled to.

#Course Added and Course Updated
- Again, a table implementation would compact the logs into a readable format.
- Combine these two actions into a Curriculum Updated log, noting all the courses(name) added, passed or failed.

Metadata must be another dropdown to ensure page compactness."

**What the AI produced:**  
The AI produced "Audit Trail v2," which dropped the redundant database triggers and implemented consolidated logging in the frontend (BulkEnrollmentTab, PreAuthorizeTab, and Tracker). It redesigned the expanded dropdowns to render rich, action-specific tables for bulk enrollments and curriculum updates, while moving raw JSON metadata into a separate collapsible section.

**What I changed / improved:** 
I identified a specific "ON CONFLICT" database error that occurred when a user accidentally entered the same email twice in a bulk enrollment batch. I had the AI implement a pre-submission validation check to catch these duplicates and provide a human-readable error message.

**What I learned / decided:**  
Nothing remarkable.

## Entry 21

**Date:** May 2, 2026
**Task:** Infinite Loading Issue When Logging in as an Unauthorized User

**Prompt given to AI:** 
"\[BUG\] Infinite loading issue when logging in through Google OAuth, if that google account is not enrolled/preauthorized. The exception must be handled and an error must be displayed in the login screen saying that the person attempting to log in is not authorized to access the application and to contact the system administration."

**What the AI produced:**  
The AI implemented a profile-verification step within the AuthContext logic. This initial solution attempted to detect missing profiles after a Google login and trigger a sign-out while setting a global error state to be displayed on the login screen.

**What I changed / improved:** 
The initial implementation suffered from a race condition where the app would redirect to the home page before the verification finished; this was fixed by delaying the redirect and adding a "Verifying..." UI state. Additionally, the OAuth popup was failing to close automatically on error, which was resolved by implementing a more aggressive window closure script. Most importantly, I pointed out that errors were still silent for accounts blocked by database triggers; the AI then implemented a postMessage bridge to transmit these backend errors from the popup window back to the main application UI.

**What I learned / decided:**  
All errors must be handled gracefully, returning a simple error description.

## Entry 22

**Date:** May 2, 2026
**Task:** Broken Search Function in Audit Trail Tab

**Prompt given to AI:** 
"\[BUG\] Search function in Manage Users page, under Audit Trail tab:

- The search bar does not seem to cover all fields. It must try to match text from all fields:
- Actor (Including Name and Role)
- Action
- Target
- Summary"

**What the AI produced:**  
The AI expanded the search filtering logic in AuditTrailTab.tsx to include actor_role, human-readable action labels (e.g., "Profile Edit"), and target_id. It also updated the search bar placeholder to accurately reflect the new searchable fields.

**What I changed / improved:** 
Requested a refinement to ensure that "System" actions (where the actor name is null in the database) are also searchable. The AI updated the filter to include a fallback that treats null actor names as "System," matching the visual representation in the table.

**What I learned / decided:**  
Nothing remarkable.

## Entry 23

**Date:** May 2, 2026
**Task:** No Persistence in Client-side Year-Sem Selection

**Prompt given to AI:** 
"\[BUG\] App fails to remember which year and semester the user is currently at, leading to the user having to manually navigate to their current year and semester.

Every browser refresh/login, the year/sem must automatically select to the current one they are editing, not on the 1st year 1st sem."

**What the AI produced:**  
The AI implemented a persistence layer for the activeYear and activeSem states using localStorage, scoped to each student's unique ID. It also integrated a "smart discovery" logic that automatically derives the current editable term from existing student records if no saved preference is found, ensuring the UI always defaults to the most relevant point of the user's progress.

**What I changed / improved:** 
No further revisions were requested; the initial solution was comprehensive and included proactive refinements to handle edge cases for new students (with no existing records) and ensured that manual navigation correctly overrides automated selection for the duration of the session.

**What I learned / decided:** 
This is another example of improving the user flow by reducing friction when navigating between elements.

## Entry 24

**Date:** May 2, 2026
**Task:** Disallowing Blocked Users From Accessing the App

**Prompt given to AI:** 
"\[BUG\] Blocked users can still login

- If a user has been blocked, they will not be able to access the application anymore.
- Logged in users will be logged out when accessing the page, redirected back to the login page.
- Attempts to login will return an error that says that the user has been blocked and to contact the system administration for assistance."

**What the AI produced:**  
The AI updated the AuthContext.tsx to include a validation check against the user's is_deleted status in the profiles table. If the user is flagged as blocked, the system executes an immediate supabase.auth.signOut() and triggers a specific error message to be displayed on the login page, effectively preventing unauthorized access for blocked accounts.

**What I changed / improved:** 
The initial implementation correctly identified that the project uses the is_deleted field as the "blocked" status based on existing logic in the UserListTab. No further revisions were requested after the first implementation as it addressed all requirements (logout on access, blocking login attempts, and specific error messaging) in one pass.

**What I learned / decided:**  
Nothing remarkable.

## Entry #

**Date:**
**Task:**

**Prompt given to AI:** 

**What the AI produced:**  

**What I changed / improved:** 

**What I learned / decided:**  

